# catquest-backend
node backend for catquest project

# setup
To store the secret api keys not in Code, I use dotenv to use a .env file. 
You need to add .env and add the apikeys. 
You can copy the .env.example and remove the .example