# catquest-backend
node backend for catquest project

## Getting Started

1. Install dependencies with npm install

2. To store the secret api keys not in Code, I use dotenv to use a .env file. 
You need to add .env and add the apikeys. 
You can copy the .env.example and remove the .example

3. Start your app with npm run dev